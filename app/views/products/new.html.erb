|<%= link_to "Manufactures", manufactures_path %>
|<%= link_to "Products", manufacture_products_path(params[:manufacture_id]) %>
|

<p> New Product of <%= @manufacture.name %> </p>

<% if @product.errors.any? %>
	<ul>
		<% @product.errors.full_messages.each do |message| %>
			<li><%= message %></li>
		<% end %>
	</ul>
<% end %>

<%= simple_form_for [@manufacture, @product] do |f| %>
	<dl>
		<% [:name, :engine, :tire].each do |product| %>
			<dt><%= product %>:</dt>
			<dd><%= f.input product %></dd>
		<% end %>
	</dl>
	<%= f.submit %>
<% end %>
